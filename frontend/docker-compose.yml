services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: sgi-frontend:prod
    container_name: sgig-frontend
    ports:
      - "${FRONTEND_HTTP_BIND:-127.0.0.1}:${FRONTEND_HTTP_PORT:-8080}:80"
    restart: unless-stopped
