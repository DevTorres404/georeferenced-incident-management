const fs = require('fs'); const files = fs.readdirSync('frontend/tests').filter(f => f.endsWith('.js')); files.forEach(f => { const p = 'frontend/tests/' + f; let c = fs.readFileSync(p, 'utf8'); if (c.includes('getStateBadgeClass: vi.fn')) { c = c.replace(/getStateBadgeClass: vi.fn\(\(\) => 'badge-secondary'\),/g, 'getStateBadgeClass: vi.fn(() => \'badge-secondary\'),\n  getStateHexColor: vi.fn(() => \'#000000\'),\n  getPriorityHexColor: vi.fn(() => \'#000000\'),'); fs.writeFileSync(p, c); } });
